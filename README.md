# JournalMd Infrastructure

Vagrant environment for development.

## Create environment

Requirements

- Ansible
- Vagrant
- VirtualBox

Run all commands in `infrastructure` directory.

Install ansible modules.

```sh
./install_requirements.sh
```

Run or create vm box.

```sh
vagrant up
```
